The templates folder includes default hooks, all of them running `ctags` on ruby files.
Could probably be improved, but works for now.

### Installation:
```bash
# In the root of a recently cloned or created repository:
cd .git/hooks
mv 
```
